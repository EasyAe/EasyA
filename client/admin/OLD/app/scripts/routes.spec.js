describe('edootechAdmin.routes', function() {
    'use strict';

    beforeEach(module('edootechAdmin'));

    it('should be defined', function() {
        expect(true)
            .toBe(true);
    });
});