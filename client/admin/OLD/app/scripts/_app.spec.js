describe('edootechAdmin', function() {
    'use strict';

    beforeEach(module('edootechAdmin'));

    it('should always work', function() {
        expect(true)
            .toBe(true);
    });
});
